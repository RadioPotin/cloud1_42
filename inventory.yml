---
all:
  vars:
    ansible_user: root
  children:
    cloud_1:
      hosts:
        cloud-1:
      vars:
        ansible_python_interpreter: /usr/bin/python3
      children:
        cloud_1:
          hosts:
            cloud-1:
              ansible_host: cloud-1-host-domain
              ansible_port: 22
